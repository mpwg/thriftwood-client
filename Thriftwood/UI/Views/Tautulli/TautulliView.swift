import SwiftUI

struct TautulliView: View {
    var body: some View {
        VStack {
            Text("Tautulli")
                .font(.largeTitle)
                .foregroundColor(.primary)
            Spacer()
        }
        .padding()
    }
}

#Preview {
    TautulliView()
}
