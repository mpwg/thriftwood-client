import SwiftUI

struct SonarrView: View {
    var body: some View {
        VStack {
            Text("Sonarr")
                .font(.largeTitle)
                .foregroundColor(.primary)
            Spacer()
        }
        .padding()
    }
}

#Preview {
    SonarrView()
}
