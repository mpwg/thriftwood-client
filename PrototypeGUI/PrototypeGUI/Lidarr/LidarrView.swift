import SwiftUI

struct LidarrView: View {
    var body: some View {
        VStack {
            Text("Lidarr")
                .font(.largeTitle)
                .foregroundColor(.primary)
            Spacer()
        }
        .padding()
    }
}

#Preview {
    LidarrView()
}
