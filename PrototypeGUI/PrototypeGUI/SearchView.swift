import SwiftUI

struct SearchView: View {
    var body: some View {
        VStack {
            Text("Search")
                .font(.largeTitle)
                .foregroundColor(.primary)
            Spacer()
        }
        .padding()
    }
}

#Preview {
    SearchView()
}
